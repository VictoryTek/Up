## What's Changed

### Bug Fixes
- **Nix**: Fixed "Everything is up to date" being shown in the headline when a backend check fails silently — errors are now surfaced as "Could not check all sources."
- **Nix**: Fixed stale evaluation and tarball cache causing the flake update check to miss available updates; the check now always fetches fresh data from upstream.
